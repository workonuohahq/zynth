"use client";
import {useEffect,useState} from "react";import Link from "next/link";import {ArrowDownToLine,BarChart3,CheckCircle2,ChevronRight,Clock3,RefreshCw,Settings2,ShieldCheck,TrendingUp,Users,XCircle} from "lucide-react";import AdminUsers from "@/components/admin-users";import ThemeSwitcher from "@/components/theme-switcher";import AdminInvestmentSettings from "@/components/admin-investment-settings";
const money=(n:any)=>`₦${Number(n||0).toLocaleString("en-NG",{minimumFractionDigits:2,maximumFractionDigits:2})}`;
export default function AdminPanel({initialData,adminEmail}:{initialData:any;adminEmail:string}){const[data,setData]=useState(initialData||{}),[tab,setTab]=useState("overview"),[reports,setReports]=useState<any[]>([]),[strategies,setStrategies]=useState<any[]>([]),[traders,setTraders]=useState<any[]>([]),[busy,setBusy]=useState(""),[notice,setNotice]=useState("");async function load(){const [q,s,t]=await Promise.all([fetch("/api/admin/settlements").then(r=>r.json()),fetch("/api/strategies").then(r=>r.json()),fetch("/api/admin/traders").then(r=>r.json())]);setReports(q.reports||[]);setStrategies(s.strategies||[]);setTraders(t.traders||[])}useEffect(()=>{load()},[]);
async function act(id:string,a:string){setBusy(id);setNotice("");let reason="";if(a==="reject")reason=window.prompt("Reason for rejection")||"Report rejected";const r=await fetch("/api/admin/settlements",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({reportId:id,action:a,reason})});const j=await r.json();setNotice(r.ok?(a==="confirm"?"Settlement confirmed and investor NAV updated.":"Report rejected."):(j.error||"Action failed."));await load();setBusy("")}
return <div className="admin-frame"><aside className="admin-sidebar"><Link href="/dashboard" className="admin-brand"><span className="brand-mark">Z</span><span>ZYNTH</span><small>ADMIN</small></Link><div className="admin-nav-label">CONTROL CENTER</div><nav className="admin-list-nav">{[{k:"overview",l:"Overview",i:BarChart3,b:data.pending_reports},{k:"settlements",l:"Settlements",i:Clock3,b:data.pending_reports},{k:"strategies",l:"Strategies",i:TrendingUp,b:data.strategies},{k:"investors",l:"Investors",i:Users,b:data.investors},{k:"traders",l:"Traders",i:ShieldCheck,b:data.traders},{k:"withdrawals",l:"Withdrawals",i:ArrowDownToLine,b:data.withdrawals_pending},{k:"settings",l:"Settings",i:Settings2}].map(({k,l,i:Icon,b})=><button key={k} className={tab===k?"admin-list-item active":"admin-list-item"} onClick={()=>setTab(k)}><span className="admin-list-icon"><Icon size={16}/></span><span className="admin-list-label">{l}</span>{b>0&&<em>{b}</em>}</button>)}</nav><div className="admin-sidebar-bottom"><span className="admin-session"><i/>Online</span><div className="admin-identity"><span className="avatar">A</span><span><b>Administrator</b><small>{adminEmail}</small></span></div></div></aside><main className="admin-main"><header className="admin-topbar"><div><div className="eyebrow-row"><span className="eyebrow">ZYNTH / ADMIN</span><span className="live-dot"><i/> CONTROL ONLINE</span></div><h1>{tab==="overview"?"Control center":tab.charAt(0).toUpperCase()+tab.slice(1)}</h1><p>Portfolio operations, strategy settlement and investor controls.</p></div><div className="admin-top-actions"><ThemeSwitcher/><button className="ghost admin-refresh" onClick={()=>location.reload()}><RefreshCw size={15}/> Refresh</button></div></header>{notice&&<div className="admin-notice">{notice}</div>}
{tab==="overview"&&<section className="admin-section"><div className="admin-kpis">{[[TrendingUp,"AUM",money(data.aumm),"PORTFOLIO"],[Users,"Investors",data.investors,"ACCOUNTS"],[ShieldCheck,"Traders",data.traders,"OPERATORS"],[Clock3,"Pending reports",data.pending_reports,"ACTION"],[ArrowDownToLine,"Withdrawals",data.withdrawals_pending,"ACTION"]].map(([Icon,label,val,tag]:any)=><div className="admin-kpi" key={label}><div className="admin-kpi-top"><span className="admin-kpi-icon"><Icon size={16}/></span><em>{tag}</em></div><small>{label}</small><b>{val}</b></div>)}</div><section className="admin-card admin-feature-card"><div className="admin-card-head"><div><span className="muted">SETTLEMENT QUEUE</span><h2>What needs attention</h2><p>Trader reports do not change investor balances until an administrator confirms them.</p></div><button className="text-action" onClick={()=>setTab("settlements")}>Open queue <ChevronRight size={13}/></button></div><div className="admin-table">{(data.pending_report_queue||[]).slice(0,6).map((r:any)=><div className="admin-row" key={r.id}><div className="admin-person"><span className="avatar">T</span><span><b>{r.strategy_name}</b><small>{r.trader_name||"Trader"} · {new Date(r.report_date).toLocaleDateString("en-NG")}</small></span></div><span>{money(r.closing_balance)}</span><span>{Number(r.return_pct||0).toFixed(2)}%</span><strong>Pending</strong></div>)}{!data.pending_report_queue?.length&&<div className="admin-empty"><CheckCircle2 size={22}/><p>No settlement reports waiting.</p></div>}</div></section></section>}
{tab==="settlements"&&<section className="admin-section"><section className="admin-card"><div className="admin-card-head"><div><span className="muted">DAILY VERIFICATION</span><h2>Settlement queue</h2><p>Confirming a report is the only action that moves strategy NAV.</p></div><span className="admin-count">{reports.length} pending</span></div><div className="admin-table">{reports.map(r=><div className="admin-row" key={r.id}><div className="admin-person"><span className="avatar">T</span><span><b>{r.strategy_name}</b><small>{r.trader_name||r.trader_email} · {new Date(r.report_date).toLocaleDateString("en-NG")}</small></span></div><span>Open {money(r.opening_balance)}</span><span>Close {money(r.closing_balance)}</span><strong>{Number(r.return_pct||0).toFixed(2)}%</strong><div className="row-actions"><button className="approve" disabled={busy===r.id} onClick={()=>act(r.id,"confirm")}><CheckCircle2 size={14}/> Confirm</button><button className="reject" disabled={busy===r.id} onClick={()=>act(r.id,"reject")}><XCircle size={14}/> Reject</button></div></div>)}{!reports.length&&<div className="admin-empty"><CheckCircle2 size={22}/><p>Settlement queue is clear.</p></div>}</div></section></section>}
{tab==="strategies"&&<section className="admin-section"><section className="admin-card"><div className="admin-card-head"><div><span className="muted">STRATEGY BOOK</span><h2>Active strategies</h2><p>Each strategy has one trader, one NAV and an auditable daily settlement history.</p></div></div><div className="admin-table">{strategies.map(s=><div className="admin-row" key={s.id}><div className="admin-person"><span className="avatar">S</span><span><b>{s.name}</b><small>{s.trader_name||"Unassigned"}</small></span></div><span>NAV ₦{Number(s.nav).toFixed(2)}</span><span>Min {money(s.minimum_investment)}</span><strong>{s.status}</strong></div>)}{!strategies.length&&<div className="admin-empty"><TrendingUp size={22}/><p>No strategies published.</p></div>}</div></section></section>}
{tab==="investors"&&<section className="admin-section"><section className="admin-card admin-users-card"><div className="admin-card-head"><div><span className="muted">INVESTOR DIRECTORY</span><h2>Investors</h2><p>Account status and portfolio controls.</p></div></div><AdminUsers initialUsers={[]}/></section></section>}
{tab==="traders"&&<section className="admin-section"><section className="admin-card"><div className="admin-card-head"><div><span className="muted">TRADER DESK</span><h2>Assigned traders</h2><p>Only assigned traders can submit daily reports for a strategy.</p></div></div><div className="admin-table">{traders.map((t:any)=><div className="admin-row" key={t.id}><div className="admin-person"><span className="avatar">T</span><span><b>{t.full_name||"Unnamed trader"}</b><small>{t.email}</small></span></div><span>Trader</span><strong>Active</strong></div>)}</div></section></section>}
{tab==="withdrawals"&&<section className="admin-section"><section className="admin-card"><div className="admin-card-head"><div><span className="muted">MONEY MOVEMENT</span><h2>Withdrawal operations</h2><p>Existing withdrawal controls remain available while profit eligibility is enforced by the Vault layer.</p></div><Link className="text-action" href="/admin/withdrawals">Open queue <ChevronRight size={13}/></Link></div></section></section>}
{tab==="settings"&&<section className="admin-section"><section className="admin-card settings-card"><div className="admin-card-head"><div><span className="muted">SYSTEM RULES</span><h2>Settlement & Vault controls</h2><p>Configure the investor engine, profit lock and operating thresholds. Changes are audited.</p></div></div><AdminInvestmentSettings/><div className="rule-list" style={{marginTop:20}}><div><span>Settlement model</span><b>Trader report → admin confirm → NAV</b></div><div><span>MT5 automation</span><b>Removed</b></div></div></section></section>}
</main></div>}