import {NextResponse} from "next/server";import {createSupabaseServerClient} from "@/lib/supabase/server";export async function GET(){const s=await createSupabaseServerClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:"Authentication required."},{status:401});await s.rpc("zynth_refresh_profit_lot_statuses");const {data,error}=await s.from("zynth_profit_lots").select("id,profit_amount,withdrawn_amount,unlock_at,status,generated_at,strategy_id").eq("user_id",user.id).order("unlock_at",{ascending:true});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({lots:data||[]});}