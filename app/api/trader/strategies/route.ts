import {NextResponse} from "next/server";import {createSupabaseServerClient} from "@/lib/supabase/server";
export async function POST(req:Request){try{const s=await createSupabaseServerClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:"Authentication required."},{status:401});const b=await req.json();const {data,error}=await s.rpc("zynth_trader_create_strategy",{p_trader_id:user.id,p_name:String(b.name||""),p_description:String(b.description||""),p_starting_balance:Number(b.startingBalance||0),p_minimum:Number(b.minimumInvestment||0),p_maximum:b.maximumInvestment?Number(b.maximumInvestment):null});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json(data);}catch(e:any){return NextResponse.json({error:e.message||"Unable to create strategy."},{status:500});}}