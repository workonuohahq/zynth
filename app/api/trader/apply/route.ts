import {NextResponse} from "next/server";import {createSupabaseServerClient} from "@/lib/supabase/server";
export async function POST(req:Request){try{const s=await createSupabaseServerClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:"Authentication required."},{status:401});const b=await req.json();const {data,error}=await s.rpc("zynth_apply_trader",{p_user_id:user.id,p_display_name:String(b.displayName||""),p_bio:String(b.bio||""),p_country:String(b.country||""),p_years:b.years?Number(b.years):null,p_markets:Array.isArray(b.markets)?b.markets:[],p_style:String(b.style||""),p_holding:String(b.holding||""),p_risk_management:String(b.riskManagement||""),p_typical_risk:b.risk?Number(b.risk):null,p_drawdown:b.drawdown?Number(b.drawdown):null,p_broker:String(b.broker||""),p_track_record:String(b.trackRecord||""),p_note:String(b.note||"")});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json(data||{success:true});}catch(e:any){return NextResponse.json({error:e.message||"Unable to submit application."},{status:500});}}