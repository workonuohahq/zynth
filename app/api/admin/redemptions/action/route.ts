import {NextResponse}from"next/server";import{createSupabaseServerClient}from"@/lib/supabase/server";
export async function POST(req:Request){const s=await createSupabaseServerClient();const{data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:"Authentication required."},{status:401});const b=await req.json().catch(()=>({}));const {data,error}=await s.rpc("zynth_admin_redemption_action",{p_admin_id:user.id,p_redemption_id:String(b.redemptionId||""),p_action:String(b.action||""),p_reason:String(b.reason||"")||null});if(error)return NextResponse.json({error:error.message,code:error.message},{status:400});return NextResponse.json(data);}