import { NextResponse } from "next/server";
import { createSupabaseServerClient } from "@/lib/supabase/server";
export async function POST(request:Request){try{const body=await request.json();const withdrawalId=String(body?.withdrawalId||"");const action=String(body?.action||"");const reason=body?.reason?String(body.reason).trim():null;if(!withdrawalId||!["review","processing","paid","reject"].includes(action))return NextResponse.json({error:"Invalid withdrawal action."},{status:400});if(action==="reject"&&!reason)return NextResponse.json({error:"A rejection reason is required."},{status:400});const s=await createSupabaseServerClient();const{data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:"Authentication required."},{status:401});const{data,error}=await s.rpc("process_withdrawal_action",{p_withdrawal_id:withdrawalId,p_action:action,p_admin_user_id:user.id,p_reason:reason});if(error){const status=/AUTHORIZATION|NOT_FOUND|INVALID_WITHDRAWAL|TRANSACTION/.test(error.message)?400:503;return NextResponse.json({error:error.message,code:error.message},{status})}return NextResponse.json(data)}catch{return NextResponse.json({error:"Unable to process withdrawal."},{status:500})}}
