"use client";
import { useEffect, useState } from "react";
export default function VaultsPage(){
 const [amount,setAmount]=useState("5500"),[message,setMessage]=useState(""),[busy,setBusy]=useState(false);
 async function create(){setBusy(true);setMessage("");const r=await fetch("/api/vault/create",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({amount:Number(amount)})});const d=await r.json();setMessage(r.ok?"Vault created successfully.":d.error||"Unable to create vault.");setBusy(false);}
 return <main className="shell"><aside className="sidebar"><div className="brand"><span className="brand-mark">Z</span><span>ZYNTH</span></div><div className="side-label">PERSONAL</div><nav><a className="nav-item" href="/dashboard">Overview</a><a className="nav-item active" href="/dashboard/vaults">Vaults</a><a className="nav-item" href="/dashboard/transactions">Transactions</a></nav></aside><section className="content"><header className="topbar"><div><span className="eyebrow">ZYNTH / VAULTS</span><h1>5-day yield vault.</h1></div><a className="fund-btn" href="/dashboard">Back</a></header><section className="grid-2"><div className="panel"><span className="muted">CREATE A CYCLE</span><h2>Lock an amount</h2><p className="copy">Your balance is checked server-side. Once enabled, the transaction and vault creation will execute atomically.</p><label className="muted">AMOUNT (₦)</label><input className="vault-input" inputMode="decimal" value={amount} onChange={e=>setAmount(e.target.value)} /><button className="primary" onClick={create} disabled={busy}>{busy?"Processing…":"Open 5-day vault"}</button>{message&&<p className="copy">{message}</p>}</div><div className="panel dark"><span className="muted">IMPORTANT</span><h2>Fixed yield is a product parameter.</h2><p className="copy">The active yield percentage is controlled from the protected system settings layer. No client can change it.</p></div></section></section></main>
}